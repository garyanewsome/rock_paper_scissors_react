{
  "name": "rps_react",
  "version": "0.1.0",
  "homepage": "http://garyanewsome.github.io/rock_paper_scissors_react",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  },
  "devDependencies": {
    "react-scripts": "1.1.5",
    "gh-pages": "^1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build" 
  }
}
